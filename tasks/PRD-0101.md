# Task PRD-0101: Scaffold packaging and config dataclasses

> PRD: PRD-01 | Depends: None
> Files: pyproject.toml, src/sdd_yolo/config.py, src/sdd_yolo/__init__.py

## What to Build
Create installable package metadata, typed config objects, and config loading utilities with validation defaults.

## Verify
```bash
uv run python -c 'from sdd_yolo.config import load_runtime_config; print(load_runtime_config("configs/default.toml").model.name)'
```

## Done When
- [ ] Code compiles/imports.
- [ ] Related tests pass.
- [ ] Interfaces match PRD acceptance criteria.
